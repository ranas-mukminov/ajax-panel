# AJAX Panel для Grafana (форк от run-as-daemon)

**Production-ready плагин панели Grafana с поддержкой Docker** — загрузка внешнего HTTP-контента (GET/POST/iframe) прямо в дашборды Grafana.

[Русский] | [English](README.md)

## Что это?

Плагин AJAX Panel предоставляет гибкий способ загрузки и отображения внешнего контента непосредственно в дашбордах Grafana. Нужно ли вам интегрировать сторонние API, встроить страницы статуса или отобразить пользовательские HTML/JSON-ответы — этот плагин позволяет делать это, не покидая среды мониторинга.

**Этот репозиторий включает production-ready Docker-конфигурацию**, которая предоставляет полностью готовый экземпляр Grafana с предустановленным и настроенным плагином AJAX Panel.

### Типичные сценарии использования

- Встраивание внешних страниц статуса или дашбордов мониторинга
- Вызов REST API и отображение ответов в различных форматах
- Отображение JSON, HTML или текста из внутренних сервисов
- Создание простых панелей управления или кастомных виджетов внутри дашбордов Grafana
- Загрузка динамических изображений или трансляций с веб-камер
- Интеграция эндпоинтов, защищённых собственной аутентификацией

## Основные возможности

- **Несколько HTTP-методов**: поддержка GET, POST и встраивания через iframe
- **Интеграция с переменными Grafana**: использование template-переменных (`$__from`, `$__to`, `$__interval` и пользовательских) в URL и параметрах
- **Гибкие режимы отображения**: рендеринг контента как HTML, plain text, JSON-дерево, preformatted-текст, изображения или Angular-шаблоны
- **Настраиваемые заголовки**: возможность установки пользовательских HTTP-заголовков и аутентификации
- **Скриптовые параметры**: JavaScript-объект для параметров с доступом к объекту `ctrl` и контексту дашборда
- **Индикаторы загрузки**: встроенный спиннер и улучшенная обработка ошибок
- **Интеграция с источниками данных**: возможность использовать конфигурации источников данных Grafana для аутентификации
- **Реализация на TypeScript**: современная, типобезопасная кодовая база

## Как это работает

AJAX Panel работает следующим образом:

1. Панель настраивается с целевым URL, HTTP-методом и опциональными параметрами
2. При обновлении дашборда или изменении временного диапазона панель отправляет запрос
3. Ответ обрабатывается в соответствии с выбранным режимом отображения
4. Template-переменные и контекст дашборда (временной диапазон, интервал) автоматически доступны для использования в запросах
5. Отрендеренный контент динамически обновляется внутри панели

## Production Docker-образ (Быстрый старт)

Этот репозиторий включает **многоэтапный Dockerfile**, который создаёт полный образ Grafana с предустановленным плагином AJAX Panel. Это рекомендуемый способ запуска плагина в production или dev-окружениях.

### Требования

- Docker (20.10+)
- Docker Compose (2.0+)

### Быстрый старт с Docker Compose

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ranas-mukminov/ajax-panel.git
   cd ajax-panel
   ```

2. Запустите сервисы:
   ```bash
   docker compose up -d
   ```

3. Откройте браузер и перейдите по адресу `http://localhost:3000`

4. Войдите с учётными данными по умолчанию:
   - **Логин**: `admin`
   - **Пароль**: `admin`

5. Создайте новый дашборд и добавьте панель **AJAX**

6. Попробуйте эндпоинты демо-бэкенда:
   - Текстовый ответ: `http://ajax-demo-backend:8080/api/demo/text`
   - JSON-ответ: `http://ajax-demo-backend:8080/api/demo/json`

Конфигурация включает:
- **grafana-ajax**: Grafana 10.4.0 с предустановленным плагином AJAX Panel
- **ajax-demo-backend**: Простой демо-HTTP-сервер для тестирования панели

### Ручная сборка и запуск Docker

Соберите образ:
```bash
docker build -t ajax-panel-grafana .
```

Запустите контейнер:
```bash
docker run -d \
  -p 3000:3000 \
  -e GF_SECURITY_ADMIN_PASSWORD=admin \
  --name grafana-ajax \
  ajax-panel-grafana
```

### Использование готовых образов из GitHub Container Registry

Образы автоматически собираются и публикуются при каждом релизе:

```bash
docker pull ghcr.io/ranas-mukminov/ajax-panel:latest
docker run -d -p 3000:3000 ghcr.io/ranas-mukminov/ajax-panel:latest
```

### Демо-бэкенд

Сервис `ajax-demo-backend` — это опциональный демонстрационный сервер, который предоставляет примеры эндпоинтов для тестирования панели AJAX:

**Доступные эндпоинты:**
- `http://ajax-demo-backend:8080/api/demo/text` — возвращает текст со временем сервера
- `http://ajax-demo-backend:8080/api/demo/json` — возвращает JSON с timestamp и случайными данными
- `http://ajax-demo-backend:8080/health` — эндпоинт health check

**Примечание**: при запуске вне Docker Compose используйте `http://localhost:8080/...` вместо имени контейнера.

## Традиционная установка (без Docker)

### Вариант 1: Установка из каталога плагинов

1. Скачайте или клонируйте этот репозиторий
2. Скопируйте папку плагина в каталог плагинов Grafana:
   ```bash
   cp -r ajax-panel /var/lib/grafana/plugins/
   ```
3. Если плагин не подписан, включите его в `grafana.ini`:
   ```ini
   [plugins]
   allow_loading_unsigned_plugins = ryantxu-ajax-panel
   ```
4. Перезапустите Grafana:
   ```bash
   systemctl restart grafana-server
   ```

> **ПРИМЕЧАНИЕ**: Скорректируйте путь к каталогу плагинов (например, `/var/lib/grafana/plugins`) и конфигурацию в соответствии с вашей установкой Grafana.

### Вариант 2: Сборка из исходников

1. Установите зависимости:
   ```bash
   yarn install
   ```
2. Соберите плагин:
   ```bash
   yarn build
   ```
3. Скопируйте каталог `dist` в папку плагинов Grafana:
   ```bash
   cp -r dist /var/lib/grafana/plugins/ryantxu-ajax-panel
   ```
4. Включите неподписанные плагины (если необходимо) и перезапустите Grafana, как описано выше

## Настройка

### Базовая настройка

1. Откройте дашборд в Grafana
2. Добавьте новую панель
3. Выберите **AJAX** в качестве типа визуализации
4. Настройте опции панели

### Опции панели

#### Метод (Method)

Выберите метод запроса:

- **GET**: стандартный HTTP GET-запрос (по умолчанию)
- **POST**: HTTP POST-запрос с параметрами в теле запроса
- **iframe**: встраивание контента через iframe

#### URL

Целевой эндпоинт для запроса. Поддерживает template-переменные Grafana:

- `$__from` — начало временного диапазона дашборда (Unix timestamp в миллисекундах)
- `$__to` — конец временного диапазона
- `$__interval` — текущий авто-интервал
- `$__interval_ms` — текущий авто-интервал в миллисекундах
- Любые пользовательские переменные дашборда

Пример:
```
https://api.example.com/status?from=$__from&to=$__to
```

#### Параметры (Parameters)

JavaScript-объект, определяющий query-параметры (для GET) или тело запроса (для POST). Доступны:

- `ctrl` — объект контроллера панели
- `ctrl.range.from` / `ctrl.range.to` — объекты временного диапазона
- `ctrl.height` — высота панели
- Template-переменные через `ctrl.template('$variable')`

Пример:
```javascript
{
  from: ctrl.range.from.format('x'),
  to: ctrl.range.to.format('x'),
  interval: ctrl.template('$__interval'),
  custom_var: ctrl.template('$my_variable')
}
```

#### Заголовки (Headers)

Пользовательские HTTP-заголовки в виде JavaScript-объекта. Полезно для аутентификации:

```javascript
{
  'Authorization': 'Bearer YOUR_TOKEN',
  'Accept': 'application/json'
}
```

#### Режим отображения (Display Mode)

Выберите, как рендерить ответ:

- **HTML**: рендеринг как HTML (небезопасный контент будет санитизирован)
- **Text**: отображение как экранированный plain text
- **JSON**: форматированный JSON с раскрываемым деревом
- **Pre**: отображение в блоке preformatted-текста
- **Image**: отображение как изображение (для бинарных image-ответов)
- **Template**: использование Angular-шаблона для форматирования ответа

### Примеры

#### Пример 1: Простой GET-запрос

**URL**: `https://httpbin.org/anything?interval=$__interval`  
**Метод**: GET  
**Параметры**:
```javascript
{
  from: ctrl.range.from.format('x'),
  to: ctrl.range.to.format('x')
}
```
**Режим отображения**: JSON

#### Пример 2: Встраивание страницы статуса через iframe

**URL**: `https://status.example.com`  
**Метод**: iframe  
**Параметры**: `{}`

## Безопасность

При использовании AJAX Panel учитывайте следующие соображения безопасности:

- **Недоверенный контент**: будьте осторожны при загрузке внешнего HTML или JavaScript. Вредоносный контент может выполняться в контексте вашего экземпляра Grafana (XSS-атаки)
- **CORS-ограничения**: cross-origin запросы могут блокироваться браузерами. Убедитесь, что целевые эндпоинты имеют соответствующие CORS-заголовки, или проксируйте запросы через источники данных Grafana
- **Аутентификация**: избегайте встраивания API-ключей или секретов напрямую в конфигурацию панели. По возможности используйте источники данных Grafana с аутентификацией
- **HTTPS**: используйте HTTPS-эндпоинты для предотвращения предупреждений о смешанном контенте и обеспечения безопасности данных
- **Внутренние эндпоинты**: предпочтительно загружать контент из доверенных внутренних сервисов, а не произвольных внешних URL
- **Раскрытие учётных данных**: конфигурация панели видна пользователям с правами редактирования дашборда

## Совместимость

Этот плагин основан на оригинальном `ryantxu/ajax-panel` и использовался с различными версиями Grafana. Кодовая база ориентирована на API Grafana 7.4+.

> **Совместимость может варьироваться в зависимости от версии Grafana. Пожалуйста, протестируйте в вашей среде перед использованием в production.**

Если вы столкнётесь с проблемами при использовании с конкретными версиями Grafana, пожалуйста, сообщите о них в issues репозитория.

## О форке и профессиональные услуги

Этот форк поддерживается **Ranas Mukminov** и командой **run-as-daemon**.

### О run-as-daemon

**run-as-daemon** ([https://run-as-daemon.ru](https://run-as-daemon.ru)) — профессиональная SRE и DevOps-консалтинговая компания, специализирующаяся на production-инфраструктуре мониторинга, контейнеризации и операционном совершенстве.

### Наши профессиональные услуги

Наша команда предоставляет enterprise-уровня услуги для организаций, которым требуются надёжные решения для мониторинга и инфраструктуры:

1. **Проектирование и развёртывание стеков мониторинга Grafana**
   - Разработка кастомных дашбордов и интеграция плагинов
   - Мультитенантные настройки Grafana с аутентификацией/авторизацией
   - Высокодоступные кластеры Grafana
   - Миграция и оптимизация Grafana Cloud

2. **Разработка и интеграция кастомных плагинов**
   - Разработка плагинов панелей, источников данных и приложений для Grafana
   - Поддержка, обновление и усиление безопасности плагинов
   - Интеграция с проприетарными API и внутренними системами
   - Оптимизация производительности для высоконагруженных сценариев

3. **Docker и Kubernetes инфраструктура**
   - Оркестрация контейнеров для стеков мониторинга
   - CI/CD-пайплайны для инфраструктуры как кода
   - Усиление безопасности и соответствие стандартам (CIS benchmarks)
   - Оптимизация ресурсов и снижение затрат

4. **SRE-практики и операционное совершенство**
   - Определение и реализация SLI/SLO/SLA
   - Проектирование стратегий алертинга и снижение усталости от алертов
   - Процедуры реагирования на инциденты и runbook'и
   - Настройка дежурных ротаций и обучение
   - Лучшие практики observability (логи, метрики, трейсы)

5. **Интеграция и миграция систем мониторинга**
   - Интеграция Prometheus, Zabbix, InfluxDB и других источников данных
   - Миграция с устаревших систем мониторинга
   - Мониторинг мультиоблачной и гибридной инфраструктуры
   - Экономически эффективная архитектура мониторинга

**Контакты**: [https://run-as-daemon.ru](https://run-as-daemon.ru)

---

## Разработка

### Требования

- Node.js (рекомендуется v14 или новее)
- Менеджер пакетов Yarn

### Процесс разработки

1. Установите зависимости:
   ```bash
   yarn install
   ```

2. Запустите режим разработки с live reload:
   ```bash
   yarn dev
   ```
   или
   ```bash
   yarn watch
   ```

3. Сборка для production:
   ```bash
   yarn build
   ```

4. Запуск тестов:
   ```bash
   yarn test
   ```

Исходный код плагина находится в каталоге `src/`, результаты сборки — в `dist/`.

## Лицензия

Проект лицензирован под **MIT License**.

Оригинальный автор: **Ryan McKinley** ([ryantxu/ajax-panel](https://github.com/ryantxu/ajax-panel))  
Сопровождающий форка: **Ranas Mukminov** ([ranas-mukminov/ajax-panel](https://github.com/ranas-mukminov/ajax-panel))

Подробности см. в файле [LICENSE](LICENSE).

## Поддержка и вклад в проект

### Поддержать проект

Если вам полезен этот плагин:
- ⭐ **Поставьте звезду** этому репозиторию на GitHub
- 🔄 **Поделитесь** им с командой и коллегами
- 🐛 **Сообщайте о проблемах** или **отправляйте pull request'ы**
- 💰 Рассмотрите поддержку через [GitHub Sponsors](https://github.com/sponsors/ranas-mukminov) (если доступно)

### Участие в разработке

Мы приветствуем вклад в проект! Пожалуйста:
1. Сделайте форк репозитория
2. Создайте feature-ветку
3. Внесите изменения с тестами
4. Отправьте pull request

Для крупных изменений сначала откройте issue для обсуждения предлагаемых изменений.
